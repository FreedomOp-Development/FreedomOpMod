package me.StevenLawson.TotalFreedomMod;

/*
* Converts any old method into strings, ints, and other objects
*/
public class TFM_Convert
{
    public static String toString(int conversion)
    {
        String converter = Integer.toString(conversion);
        return converter;
    }
}
